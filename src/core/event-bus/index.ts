/**
 * EventBus（mitt）封装占位。
 *
 * 职责：
 * - 提供跨插件通信通道（PRD 强制：插件间禁止直接 import 对方 store）。
 * - 集中维护事件名与（后续的）类型定义，避免事件命名冲突。
 *
 * 约束：
 * - 事件命名规范：`{插件ID}:{动词}:{名词}`。
 * - 事件白名单/类型应与 `CONTEXT.md` 中的事件协议表保持一致。
 */

