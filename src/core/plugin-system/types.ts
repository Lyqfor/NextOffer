/**
 * 插件系统类型定义（占位）。
 *
 * 职责：
 * - 定义统一的 Plugin 接口、插件元数据、导航项、PluginContext 等。
 * - 作为“跨插件解耦”的契约：主应用与插件只通过接口交互。
 *
 * 约束（PRD 强制）：
 * - 插件间禁止直接 import 对方 store；跨插件交互走 EventBus。
 *
 * 参考：
 * - PRD 4.1 Plugin 接口定义。
 */

