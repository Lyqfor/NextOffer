<template>
  <div id="app">NextOffer</div>
</template>

<script setup lang="ts">
/**
 * 根组件占位。
 *
 * 职责（后续实现）：
 * - 承载 Naive UI 的全局 ConfigProvider（主题/语言/全局样式）。
 * - 作为布局容器：通常会渲染 `layouts/MainLayout.vue` 或 `layouts/BrowserLayout.vue`，
 *   并在内部放置 RouterView。
 *
 * 约束：
 * - 具体业务页面归属于插件（`plugins/*/views`），不要在这里写业务 UI。
 */
</script>

