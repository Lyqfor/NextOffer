/**
 * 插件注册中心 Store（占位）。
 *
 * 职责：
 * - 为 UI 层提供“已注册插件列表 / 导航项 / 角标”等响应式数据来源。
 * - 与 `core/plugin-system/registry.ts` 协同：registry 负责安装/生命周期，store 负责可观察状态。
 *
 * 边界：
 * - 不直接持有插件业务数据；只存插件元信息与导航展示所需数据。
 */

