<template>
  <div>ThemeToggle（占位）</div>
</template>

<script setup lang="ts">
/**
 * 主题切换组件（占位）。
 *
 * 职责：
 * - 在 light/dark/system 三种主题间切换。
 * - 与全局设置（`stores/app.ts`）联动，并驱动 Naive UI ConfigProvider。
 *
 * 边界：
 * - 只处理主题切换 UI/交互；持久化写入由设置服务/Command 层负责（后续）。
 */
</script>

