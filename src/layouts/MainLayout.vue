<template>
  <div>MainLayout（占位）</div>
</template>

<script setup lang="ts">
/**
 * 主布局（MainLayout）占位。
 *
 * 职责（按 PRD 模块 0）：
 * - 左侧：由 PluginRegistry 动态生成的全局导航（按 plugin meta.order 排序）。
 * - 顶部：全局搜索入口（Cmd/Ctrl+K）、主题切换、设置入口等。
 * - 主内容区：RouterView 渲染当前插件页面。
 *
 * 约束：
 * - 导航项、角标（badge）等由插件声明与提供；布局只负责渲染与交互框架。
 */
</script>

