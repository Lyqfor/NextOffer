/**
 * Profile 插件路由声明（占位）。
 *
 * 职责：
 * - 声明本插件注入的路由（/profile/basic、/profile/education...）。
 * - 由 PluginRegistry 在注册插件时注入到全局 Router。
 *
 * 边界：
 * - 不在这里初始化 store 或事件监听；生命周期逻辑放在 `index.ts` 的 install()。
 */

