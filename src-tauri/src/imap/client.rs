//! IMAP 客户端与连接管理（占位）。
//!
//! 职责：
//! - 根据 `inbox_accounts` 中的 IMAP Host/Port/SSL 配置建立连接。
//! - 定时拉取 UNSEEN 邮件（或按时间范围拉取最近 N 天邮件）。
//! - 提供“连接测试”能力（添加账号时使用）。
//!
//! 约束：
//! - 不负责解析业务含义（交给 `parser.rs`）。
//! - 不持久化凭据；凭据由上层从 Keychain 提供给本模块使用。

// 占位：后续接入 imap crate 并实现连接/拉取。

