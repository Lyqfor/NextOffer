fn main() {
  // NextOffer Tauri 主进程入口（占位）。
  //
  // 职责：
  // - 初始化 Tauri 应用（窗口/插件/权限/CSP 等）。
  // - 注册 commands（src/commands/ 下分模块）。
  // - 初始化全局服务（SQLite、通知、Keychain 等）并注入到 command handler。
  //
  // 约束：
  // - 业务逻辑应位于各模块内，不在 main 中堆实现。
}

