//! Inbox（邮件中心）相关 Tauri Commands（占位）。
//!
//! 职责：
//! - 邮箱账号管理（添加/删除/测试连接/启停）。
//! - 触发同步：调用 IMAP 客户端拉取邮件并写入解析结果表。
//! - 解析结果的查询/修正/关联看板卡片。
//!
//! 安全约束：
//! - IMAP 密码/授权码必须存 Keychain；数据库仅存 `keychain_key` 引用。

