//! Browser（投递浏览器/账号）相关 Tauri Commands（占位）。
//!
//! 职责：
//! - 账号元数据 CRUD（公司名、登录 URL、用户名、keychain_key 等）。
//! - 密码写入/读取 Keychain（只在需要时读取；数据库不落密码）。
//! - 更新最近使用时间等轻量操作。
//!
//! 安全约束：
//! - 禁止把密码回写到 SQLite 或返回给前端持久化；只用于运行时能力（如自动登录预留）。

